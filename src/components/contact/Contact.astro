---
import Container from "@common/Container.astro";
import Section from "@common/Section.astro";
import SectionTitle from "@common/SectionTitle.astro";
import Instagram from "@components/icons/Instagram.astro";
import Facebook from "@components/icons/Facebook.astro";
import Whatsapp from "@components/icons/Whatsapp.astro";

import { gmLinks } from "@utils/index";
import { ContactForm } from "./ContactForm";
---

<Section background="primary" id="contact">
  <Container>
    <SectionTitle
      >Agenda Tu Consulta Laboral Gratuita en CDMX - Te Respondemos en 24 Horas</SectionTitle
    >
    <p class="gm_contact-text reveal">
      En GM Abogados Laboralistas sabemos que tu tiempo es valioso y tu situación es urgente. Por eso ofrecemos consultas gratuitas donde evaluamos tu caso de despido injustificado, te decimos exactamente cuánto puedes recuperar y diseñamos la mejor estrategia legal para ti. <strong>Sin costo, sin compromiso.</strong> Solo recuperamos nuestro honorario cuando tú recuperas tu dinero.
    </p>
    <div class="gm_contact-container">
      <ContactForm client:only />

      <aside class="gm_contact-secondary reveal">
        <div class="gm_contact-secondary-item">
          <p>Escribenos a</p>
          <a href="mailto:iang27191@gmail.com"
            >iang27191@gmail.com <span></span></a
          >
        </div>
        <div class="gm_contact-secondary-item">
          <p>Llamanos al</p>
          <a href="tel:+525566011051">55 66011051 <span></span></a>
        </div>
        <div class="gm_contact-socials">
          <a
            href={gmLinks.instagram}
            target="_blank"
            name="Link para ir al Instagram del Lic. Gonzalez Mora"
            ><Instagram /></a
          >
          <a
            href={gmLinks.facebook}
            target="_blank"
            name="Link para ir Facebook al del Lic. Gonzalez Mora"
            ><Facebook /></a
          >
          <a
            href={gmLinks.whatsapp}
            target="_blank"
            name="Link para ir al Whatsapp del Lic. Gonzalez Mora"
            ><Whatsapp /></a
          >
        </div>
      </aside>
    </div>
  </Container>
</Section>

<style>
  .gm_contact-text {
    font-size: 1.2rem;
    margin-top: 1.5rem;
    font-weight: 500;
    line-height: 1.25;
    margin-top: 1.5rem;

    @media screen and (width >= 768px) {
      max-width: 35rem;
      font-size: 1.4rem;
    }
  }

  .gm_contact-container {
    padding: 2.8rem 0;

    @media screen and (width >= 1024px) {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12rem;
      align-items: center;
    }
  }

  .gm_contact-secondary {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    margin-top: 3rem;

    @media screen and (width >= 1024px) {
      margin-top: 0;
    }
  }

  .gm_contact-secondary-item {
    p {
      font-size: 1.3rem;
      font-family: var(--gm-family-title);
      color: var(--gm-primary);
      margin-bottom: 0.6rem;
    }

    a {
      font-size: 1.2rem;
      color: var(--gm-title-text);
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 1rem;
      cursor: pointer;
      text-decoration: none;

      span {
        display: block;
        width: 0.7rem;
        height: 0.7rem;
        background: var(--gm-accent-gradient);
        border-radius: 999px;
        animation: pulse 1.5s infinite;
      }
    }
  }

  .gm_contact-socials {
    display: flex;
    gap: 1rem;

    a {
      background: var(--gm-accent-gradient);
      padding: 0.8rem;
      border-radius: 999px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--gm-bg-secondary);
      box-shadow: var(--gm-button-shadow);
      scale: 1.04;
      transition: scale 150ms ease-in-out;

      &:hover {
        scale: 1.1;
      }

      &:active {
        scale: 1;
      }
    }
  }
</style>
